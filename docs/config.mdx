# Supported LLM Models ENT Config

The following is an on-site configuration. Admins should pick a value from the table for `chatModel` to configure their Chat model.

<FeatureParity type='configuration' />
